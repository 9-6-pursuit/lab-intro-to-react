const NavBar = () => {
  return (
    <div className="nav-bar">
      <p>Logo</p>
      <div className="buttons">
        <button>Home</button>
        <button>About</button>
        <button>Log in</button>
      </div>
    </div>
  );
};

export default NavBar;